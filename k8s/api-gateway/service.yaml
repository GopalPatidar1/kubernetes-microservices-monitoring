apiVersion: v1
kind: Service

metadata:
  name: api-gateway-service

spec:
  type: NodePort

  selector:
    app: api-gateway-service

  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
