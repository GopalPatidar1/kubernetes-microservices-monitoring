apiVersion: apps/v1
kind: Deployment

metadata:
  name: api-gateway-service

spec:
  replicas: 1

  selector:
    matchLabels:
      app: api-gateway-service

  template:
    metadata:
      labels:
        app: api-gateway-service

    spec:
      containers:
        - name: api-gateway-service
          image: api-gateway-service

          imagePullPolicy: Never

          ports:
            - containerPort: 3000

          env:
            - name: REDIS_HOST
              value: redis

            - name: REDIS_PORT
              value: "6379"

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"

            limits:
              cpu: "500m"
              memory: "512Mi"
